learn about formik

2.react router dom->
  2.1 create browser router =   create the router
    2.1.2
    other methods to ceat router=
    2.1.2.1 hash router-it will use the hash (#) portion of the URL to manage the "application URL".
  
    2.1.2.2 memory rouet-it's primarily useful for testing and component development 
  
  2.2 RouterProvider=provide the router created by above comp to render
  
  2.3 useroutererror->hook which gives info about error

  2.4 link =

  2.5 oulet=used in nesting we just pass outlet inside haeder footer and it cjanges acc to route  
  